Intro to Statistical Modeling Ch. 3 Prob. 13 
========================================================
```{r include=FALSE}
require(mosaic)
require(ScoreR)
startProblem("SM-3-P13-SD")
```

```{r include=FALSE}
prob1=selectSet(name="side.by.side", totalPts=1, "Categorical only"=FALSE, "Quantitative only"=FALSE, "One categorical and one quantitative"=TRUE, "Varies according to situation"=FALSE)
```
* From what kinds of variables would side-by-side boxplots be
  generated? `r I(prob1)`

```{r include=FALSE}
prob2=selectSet(name="histogram", totalPts=1, "Categorical only"=FALSE, "Quantitative only"=TRUE, "One categorical and one quantitative"=FALSE, "Varies according to situation"=FALSE)
```
* From what kinds of variables would a histogram be generated? `r I(prob2)`

`r I(endProblem())`